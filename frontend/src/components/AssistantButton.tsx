// 全站悬浮助理空壳：A 阶段接对话，本期仅占位
export function AssistantButton() {
  return (
    <button
      className="assistant-fab"
      aria-label="助理"
      onClick={() => alert("即将上线")}
    >
      💬
    </button>
  );
}
